---
const clientTypes = [
  {
    title: 'Startups',
    description: 'Avancez rapidement sans rien casser. Tests automatisés, CI/CD et surveillance intelligente.',
    features: ['Prototypage rapide', 'Infrastructure évolutive', 'Optimisation des coûts']
  },
  {
    title: 'Scale-ups',
    description: 'Gérez la croissance sans augmenter les effectifs. Des systèmes intelligents qui évoluent avec vous.',
    features: ['Optimisation des performances', 'Outils de productivité', 'Gestion de la dette technique']
  },
  {
    title: 'Entreprises',
    description: 'Automatisation de niveau entreprise avec sécurité et conformité intégrées.',
    features: ['Modernisation du legacy', 'Automatisation de conformité', 'Coordination multi-équipes']
  },
  {
    title: 'Équipes Dev',
    description: 'Des outils que les développeurs veulent vraiment utiliser. Fini le travail fastidieux.',
    features: ['Automatisation des revues', 'Génération de documentation', 'Gestion des dépendances']
  }
];
---

<section id="clients" class="py-24 bg-gradient-subtle relative">
  <div class="container mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-neutral-100 mb-4 tracking-tightest">
        Qui nous servons
      </h2>
      <p class="text-xl text-neutral-400 max-w-2xl mx-auto">
        Des startups ambitieuses aux entreprises établies, nous aidons les équipes d'ingénierie à livrer plus rapidement.
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 max-w-5xl mx-auto">
      {clientTypes.map((client) => (
        <div class="p-8 rounded-2xl bg-gradient-to-br from-neutral-900/80 via-neutral-925/70 to-neutral-900/80 border border-neutral-800/50 hover:border-blue-700/40 hover:shadow-xl hover:shadow-blue-900/20 transition-all backdrop-blur-sm">
          <h3 class="text-2xl font-semibold text-neutral-100 mb-3">
            {client.title}
          </h3>
          <p class="text-neutral-400 mb-4 leading-relaxed">
            {client.description}
          </p>
          <ul class="space-y-2">
            {client.features.map((feature) => (
              <li class="flex items-center text-neutral-500">
                <svg class="w-5 h-5 mr-2 text-neutral-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4"></path>
                </svg>
                {feature}
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
  </div>
</section>